// create-user.use-case.ts — no lo necesitás.
// UserFinderAdapter.create() ya crea users desde auth.
// Si más adelante querés un endpoint admin POST /users para crear users desde el panel, ahí lo agregás.
// MVP no.

// update-profile.request.dto.ts + update-profile.use-case.ts — backlog.
// Cambiar email/password propio. No entra en MVP.

// list-users.use-case.ts — backlog también (admin).
